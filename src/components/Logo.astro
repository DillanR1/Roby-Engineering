<div class="hero-branding">
  <img 
    src="/public/skull-logo.png" 
    alt="Roby Engineering" 
    class="brand-skull"
  />
  <div class="brand-text">
    <h1 class="brand-name">
      ROBY <span class="engineering">ENGINEERING</span>
    </h1>
    <p class="brand-tagline">Vis per Scientiam</p>
    <p class="brand-stack">React <span class="divider">|</span> Astro <span class="divider">|</span> Python Proficiencies</p>
    <p class="brand-mission">Builder of Badass Sites</p>
  </div>
  <span class="trademark">™</span>
</div>

<style>
.hero-branding {
  position: relative;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 4rem 2rem;
  min-height: 80vh;
  text-align: center;
  animation: fadeIn 1s ease-out;
}

/* MASSIVE SKULL */
.brand-skull {
  width: 350px;
  height: auto;
  margin-bottom: 3rem;
  filter: drop-shadow(0 0 40px rgba(255, 102, 0, 0.5));
  animation: subtlePulse 4s ease-in-out infinite, floatIn 1.2s ease-out;
  transition: all 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  border-radius: 2rem;
}

.brand-skull:hover {
  transform: scale(1.05);
  filter: drop-shadow(0 0 60px rgba(255, 102, 0, 0.7));
}

/* BRAND TEXT CONTAINER */
.brand-text {
  max-width: 900px;
}

/* MAIN NAME - NO TM */
.brand-name {
  font-size: clamp(2.5rem, 6vw, 5rem);
  font-weight: 900;
  letter-spacing: -0.02em;
  line-height: 1.1;
  margin: 0 0 1rem 0;
  color: #ffffff;
  text-transform: uppercase;
}

.brand-name .engineering {
  color: #ff6600;
  display: inline-block;
  animation: glowPulse 3s ease-in-out infinite;
}

/* VIS PER SCIENTIAM - CLEAN, NO UNDERLINE */
.brand-tagline {
  font-size: clamp(1.5rem, 3vw, 2.5rem);
  font-weight: 300;
  font-style: italic;
  color: rgba(255, 255, 255, 0.85);
  margin: 0 0 1.5rem 0;
  letter-spacing: 0.05em;
}

/* TECH STACK */
.brand-stack {
  font-size: clamp(1rem, 2vw, 1.25rem);
  color: rgba(255, 255, 255, 0.7);
  font-weight: 500;
  margin: 0 0 1rem 0;
  letter-spacing: 0.1em;
}

.brand-stack .divider {
  color: #ff6600;
  margin: 0 0.75rem;
}

/* MISSION STATEMENT */
.brand-mission {
  font-size: clamp(1.25rem, 2.5vw, 1.75rem);
  color: rgba(255, 255, 255, 0.9);
  font-weight: 600;
  margin: 1rem 0 0 0;
  letter-spacing: 0.03em;
}

/* TRADEMARK - SUBTLE CORNER */
.trademark {
  position: absolute;
  top: 2rem;
  right: 2rem;
  font-size: 0.875rem;
  color: rgba(255, 255, 255, 0.4);
  font-weight: 300;
}

/* ANIMATIONS */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes floatIn {
  from {
    opacity: 0;
    transform: translateY(-50px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes subtlePulse {
  0%, 100% {
    filter: drop-shadow(0 0 40px rgba(255, 102, 0, 0.5));
  }
  50% {
    filter: drop-shadow(0 0 50px rgba(255, 102, 0, 0.6));
  }
}

@keyframes glowPulse {
  0%, 100% {
    text-shadow: 0 0 20px rgba(255, 102, 0, 0.6);
  }
  50% {
    text-shadow: 0 0 30px rgba(255, 102, 0, 0.8);
  }
}

/* MOBILE RESPONSIVE */
@media (max-width: 768px) {
  .hero-branding {
    padding: 3rem 1.5rem;
    min-height: 70vh;
  }
  
  .brand-skull {
    width: 250px;
    margin-bottom: 2rem;
    
  }
  
  .brand-name {
    font-size: 2rem;
  }
  
  .brand-tagline {
    font-size: 1.25rem;
  }
  
  .brand-stack {
    font-size: 0.9rem;
  }
  
  .brand-mission {
    font-size: 1.1rem;
  }
  
  .trademark {
    top: 1rem;
    right: 1rem;
    font-size: 0.75rem;
  }
}

/* TABLET */
@media (min-width: 769px) and (max-width: 1024px) {
  .brand-skull {
    width: 300px;
  }
}

/* PREMIUM GLASS BACKDROP (OPTIONAL ENHANCEMENT) */
.hero-branding::before {
  content: "";
  position: absolute;
  inset: 0;
  background: radial-gradient(
    ellipse at center,
    rgba(255, 102, 0, 0.03) 0%,
    transparent 70%
  );
  pointer-events: none;
  z-index: -1;
}
</style>